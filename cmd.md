```
uv run uvicorn app.main:app --reload
uv run pytest
```

### 1. `uv run uvicorn app.main:app --reload`의 의미
이 명령어는 **개발 서버(Development Server)를 가동**시키는 도구입니다.

* **uvicorn:** 파이썬 웹 애플리케이션을 실행해주는 '엔진'입니다.
* **app.main:app:** `app/main.py` 파일 안에 있는 `app`이라는 FastAPI 객체를 실행하라는 뜻입니다.
* **--reload:** 코드를 수정하고 저장하면 서버를 자동으로 껐다 켜주는 기능입니다.
* **결과:** `http://127.0.0.1:8000` 주소로 접속 가능한 **살아있는 서버**가 뜹니다. 이때는 Swagger UI(`docs`)를 보거나 Postman으로 요청을 보낼 수 있지만, 작성해둔 테스트 코드가 자동으로 돌아가지는 않습니다.

---

### 2. 테스트를 실행하려면?
파이썬에서 테스트를 실행할 때는 보통 **`pytest`**라는 도구를 사용합니다.

* **명령어:** `uv run pytest`
* **동작:** 프로젝트 폴더 내에서 `test_*.py` 또는 `*_test.py`라는 이름의 파일을 모두 찾아 그 안에 있는 테스트 함수들을 자동으로 실행합니다.
* **결과:** 서버를 띄우지 않고, 코드들이 의도대로 작동하는지 `PASSED` 또는 `FAILED` 결과만 깔끔하게 보여줍니다.

---

### 3. 두 도구의 결정적 차이

| 구분 | `uvicorn` (서버 실행) | `pytest` (테스트 실행) |
| :--- | :--- | :--- |
| **목적** | 실제 서비스 운영 및 API 호출 테스트 | 코드의 논리적 무결성 검증 |
| **상태** | 서버가 계속 켜져 있음 (수동 종료 필요) | 테스트가 끝나면 즉시 종료됨 |
| **확인 방법** | 웹 브라우저, Postman, Swagger | 터미널에 찍히는 초록색/빨간색 결과 |
| **사용 시점** | 기능을 직접 써보며 개발할 때 | 리팩토링 후 기존 기능이 깨졌는지 확인할 때 |



---

### 💡꿀팁
리팩토링 중이시라면 터미널 창을 두 개 띄워놓고 작업하는 것이 정석입니다.

1.  **터미널 1:** `uv run uvicorn ...` (서버를 띄워놓고 실제 연동 확인)
2.  **터미널 2:** `uv run pytest` (수시로 입력해서 내가 고친 코드가 기존 테스트를 통과하는지 확인)

